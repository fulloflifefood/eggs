.grid_6
  %p{:style => "color: green"}= flash[:notice]
  = link_to "Record New (Non-EggBasket) Transactions", {:controller => "subscription_transactions", :action => "new_many", :farm_id => @farm.id, :product_id => @product.id}, :class => "button green"
  %h2#page-heading= "#{@product.name} Subscribers:"
  %table
    %thead
      %tr
        %th Name
        %th Balance
    - @subscriptions.each do |subscription|
      %tr
        %td= link_to subscription.account.member.last_name, subscription_transactions_path(:farm_id => @farm.id, :subscription_id => subscription.id)
        %td= subscription.current_balance


  .grid_5 
    %div
      %h4 FAQ:
      %p
        %h6 Q: How do I add a new subscriber and give them the initial (100) credits?
        A: Email
        %a{:href => "mailto:kathryn@kathrynaaker.com"}Kathryn!
      %p
        %h6 Q: Oops, I made a mistake (credited the wrong person, put in the wrong number, wrong pickup location, etc.  How do I fix this?
        A: Email
        %a{:href => "mailto:kathryn@kathrynaaker.com"}Kathryn!
